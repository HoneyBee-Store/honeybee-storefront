{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Comment": "Local development only, and safe to commit: a dedicated low-privilege role on a local database, not the postgres superuser. Production supplies ConnectionStrings__Default as an environment variable.",
    "Default": "Host=localhost;Port=5432;Database=honeybee;Username=honeybee;Password=honeybee_dev"
  }
}
