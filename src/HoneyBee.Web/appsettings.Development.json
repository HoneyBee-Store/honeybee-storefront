{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ConnectionStrings": {
    "Comment": "Local development only, and safe to commit: LocalDB with Windows authentication carries no secret. It lives here rather than in User Secrets so every way of launching the app agrees on one database. MSSQLLocalDB is the default instance Visual Studio and the SQL tools already use.",
    "Default": "Server=(localdb)\\MSSQLLocalDB;Database=honeybee;Trusted_Connection=True;MultipleActiveResultSets=true;Connect Timeout=60"
  }
}
