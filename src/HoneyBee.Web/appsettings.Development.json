{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Comment": "Local development only, and safe to commit: LocalDB with Windows authentication carries no credentials. Production supplies ConnectionStrings__Default as an environment variable. A fresh database name because the old one still holds the migration history from before the PostgreSQL detour.",
    "Default": "Server=(localdb)\\MSSQLLocalDB;Database=honeybeeshop;Trusted_Connection=True;MultipleActiveResultSets=true;Connect Timeout=60"
  }
}
