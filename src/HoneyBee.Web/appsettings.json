{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Comment": "Real connection strings never live in this file — it is committed to git. Development uses User Secrets, production uses an environment variable. See README.",
    "Default": ""
  }
}
